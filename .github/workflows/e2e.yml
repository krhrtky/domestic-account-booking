name: E2E Tests (Deprecated - Use test-e2e.yml)

on:
  workflow_call:

jobs:
  redirect:
    name: Redirect to test-e2e.yml
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️ This workflow is deprecated."
          echo "Please use .github/workflows/test-e2e.yml instead."
          echo "This file will be removed in a future update."
